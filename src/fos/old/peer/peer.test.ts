import { Fos } from '..'

describe('peer basics', () => {

  test.skip('can add peer and ', () => {
    const fos = Fos()
    // const otherFos =  Fos()
    // const otherFosPeer = JsPeer(otherFos)
    // fos.addPeer(otherFosPeer)
  })

  // test('can add peer and ', () => {
  //   const fos = Fos()
  //   const otherFos =  Fos()
  //   const otherFosPeer = JsPeer(otherFos)
  //   fos.addPeer(otherFosPeer)

  //   const groupClient = fos.createGroup('group1')

  //   groupClient.invitePeer(otherFosPeer)

  //   otherFos.getChildren()[0].mutate(acceptInvitation)
  //   otherFos.run()

  //   groupClient.createTask('A')
  //   groupClient.proposeChanges()

  //   group.proposeCoreChanges()
  // })



})